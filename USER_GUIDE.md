# 用户指南（User Guide）

类 Typora 的 WYSIWYG（所见即所得）Markdown 写作工具。**本地优先**：数据只存于你的浏览器，离线可用，无需注册。

---

## 1. 功能导览

### 写作
- 顶部工具栏：标题（H1-H3）、加粗/斜体/行内代码、链接、图片、引用、代码块、分割线、列表、表格一键插入
- 选中文本即弹出格式浮层（加粗/斜体/行内代码/链接）；输入 `/` 唤起斜杠菜单
- 所见即所得：输入 Markdown 语法即时渲染（# 标题、**加粗**、*斜体*、`代码`、> 引用、- 列表）
- 表格：`/表格` 或粘贴 TSV/CSV 插入；悬停表格边缘出现 ➕ 增行/增列手柄
- 图片：点击图片块上传，文件存入当前文档的 `assets/` 相对路径
- 数学公式：KaTeX 渲染（`$...$` 行内 / `$$...$$` 块）
- 流程图：mermaid 代码块自动渲染为图表

### 文档管理（左侧边栏）
- 新建文档 / 文件夹（工具栏 ➕ 按钮）
- 重命名（悬停 ✎） / 删除（悬停 🗑，进回收站）
- 大纲：右侧边栏实时显示当前文档标题结构

### 搜索与替换
- 当前文档查找/替换：`Ctrl+F`
- 全文搜索（所有文档）：`Ctrl+Shift+F`

### 主题与视图
- 主题：`☀️ 亮色 / 🌙 暗色 / 🌓 跟随系统`（工具栏或设置面板）
- 无干扰模式：`🖥 标准 / 👁 聚焦 / 🎯 无干扰`（隐藏侧边栏）

### 导出与备份
- 导出 HTML：工具栏按钮（当前文档）
- 导出 zip：工具栏按钮
- 数据备份/恢复：设置 → 数据备份（全量 JSON 导出/导入）

---

## 2. 快捷键表

| 快捷键 | 功能 |
|---|---|
| `Ctrl+F` | 查找（当前文档） |
| `Ctrl+Shift+F` | 全文搜索 |
| `Ctrl+B` | 加粗 |
| `Ctrl+I` | 斜体 |
| `Ctrl+Z` / `Ctrl+Y` | 撤销 / 重做 |
| `Esc` | 关闭面板 / 退出搜索 |
| `/`（输入） | 唤起插入菜单（表格/图片/标题…） |
| `Enter` | 换行；空段再按 → 退出列表/引用 |
| `Shift+Tab` | 列表项取消缩进 |

---

## 3. 数据备份与恢复（重要）

> 你的数据存在**浏览器**里。换设备、清缓存、换浏览器都会丢失——建议定期导出备份。

**导出**：工具栏「⚙ 设置」→「数据备份」→「⬇ 导出备份」，得到 `.json` 文件。

**恢复**：同上 →「⬆ 导入恢复」，选择备份文件。导入后刷新页面生效。

**导入 .md**：工具栏「导入 .md」选择本地 Markdown 文件，即成为一篇新文档。

---

## 4. 常见问题（FAQ）

**Q: 数据会同步到服务器吗？**
不会。所有数据只在你的浏览器本地（IndexedDB），详见 [PRIVACY.md](PRIVACY.md)。

**Q: 换电脑/浏览器，文档还能找回来吗？**
只要之前导出过备份（设置 → 数据备份 → 导出），在新浏览器导入即可。没有备份则无法恢复。

**Q: 表格的增行/增列手柄在哪？**
把鼠标移到表格最下方或最右侧边缘，会出现 ➕ 圆形按钮；点击即可添加。

**Q: 删掉的文档还能找回吗？**
能。删除是「软删除」——进入回收站，保留 30 天；期间可恢复。30 天后自动彻底清理。

**Q: 图片存在哪里？**
上传的图片以相对路径存进当前文档所在库的 IndexedDB 资产表，随文档导出一起打包。

**Q: 清除浏览器数据会怎样？**
本站点的全部文档、图片、版本、搜索索引一并删除，且无法从服务器恢复。务必先导出备份。

---

## 5. 版本历史

- **v1.0**（Phase 5 完成）：WYSIWYG 编辑 / 文档树 / 表格图片 / 主题 / 全文搜索 / 版本快照 / 回收站 / 备份恢复 / 首启引导
